<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appbar_scrolling_view_behavior"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClienteHola"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".TRINOMIO"
            android:exported="false" />
        <activity
            android:name=".ISOSCELES"
            android:exported="false" />
        <activity
            android:name=".PENTAGONO"
            android:exported="false" />
        <activity
            android:name=".CIRCULO"
            android:exported="false" />
        <activity
            android:name=".SUMAPARAMETRO"
            android:exported="false" />
        <activity
            android:name=".SUMASIMPLE"
            android:exported="false" />
        <activity
            android:name=".MENSAJE"
            android:exported="false" /> <!-- Actividad MENU como la actividad principal -->
        <activity
            android:name=".MENU"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>